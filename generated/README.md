# generated/

Scratch output folder. `scripts/generate_content_brief.py` writes finished
content briefs here. Nothing in this folder is committed (see `.gitignore`)
— it's regenerated from `content_calendar/calendar.csv` + `templates/` on
demand, so there's nothing worth keeping in git.
